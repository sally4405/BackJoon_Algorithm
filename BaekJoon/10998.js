// https://www.acmicpc.net/problem/10998
// A×B

// const input = require("fs").readFileSync("/dev/stdin").toString().split(" ").map((n) => parseInt(n));
const input = "1 2".split(" ").map((n) => parseInt(n));
console.log(input[0] * input[1]);
